<%= form_for @comment, :url => trail_comments_path do |f| %>
  <p>
    <%= f.text_area :content %>
    <%= f.hidden_field :trail_id, :value => params[:trail_id] %>
  </p>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>